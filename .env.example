# Copy to .env on the *friend* laptop only if she is talking to Xiao's ship.

# Leave unset to use this laptop's own server (via the Vite proxy).
# VITE_SOCKET_URL=http://192.168.1.12:43128

# Track A (Xiao) — optional Grok Voice after the Cursor workshop.
# VITE_XAI_API_KEY=
# VITE_GROK_TTS_URL=
